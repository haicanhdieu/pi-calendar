# Team override for bmad-ux. See .claude/skills/bmad-ux/customize.toml for full surface.
#
# Group artifacts slug-then-subtype under the shared planning-artifacts bucket,
# flattened (no dated run folder) since this repo has one PRD per slug. See AGENTS.md.
[workflow]
ux_output_path = "{planning_artifacts}/pico-w-calendar-clock/ux-designs"
run_folder_pattern = ""
