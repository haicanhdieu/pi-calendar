# Team override for bmad-architecture. See .claude/skills/bmad-architecture/customize.toml for full surface.
#
# Group artifacts slug-then-subtype under the shared planning-artifacts bucket,
# flattened (no dated run folder). Path points at the currently active slug (alert-clock); see AGENTS.md.
[workflow]
spine_output_path = "{planning_artifacts}/alert-clock/architecture"
run_folder_pattern = ""
