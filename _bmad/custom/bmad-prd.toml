# Team override for bmad-prd. See .claude/skills/bmad-prd/customize.toml for full surface.
#
# Group artifacts slug-then-subtype under the shared planning-artifacts bucket,
# flattened (no dated run folder) since this repo has one PRD per slug. See AGENTS.md.
[workflow]
prd_output_path = "{planning_artifacts}/pico-w-calendar-clock/prds"
run_folder_pattern = ""
