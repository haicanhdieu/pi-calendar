---
title: 'Sound a bounded tut cadence through the buzzer port'
type: 'feature'
created: '2026-09-21'
status: 'done'
review_loop_iteration: 0
followup_review_recommended: false
context:
  - '_bmad-output/implementation-artifacts/alert-clock/epic-2-context.md'
warnings: []
deferred: []
baseline_revision: '589488e3a11bc49198f36d80ddfc96e94b0f467d'
---

<intent-contract>

## Intent

**Problem:** The device has no bounded buzzer adapter, so future alert occurrences cannot produce audible output without risking blocking timing or unsafe GPIO ownership.

**Approach:** Add one device-layer `BuzzerPort` with low-first initialization, active-high non-blocking cadence, wrap-safe deadlines, bounded edge resynchronization, idempotent silence, and named failure results. Construct it from main using named config constants.

## Boundaries & Constraints

**Always:** Use GP15, 180 ms high, 220 ms low, active-high injection from `config.py`; use `ticks_add`/`ticks_diff`; drive low before exposure; keep failure results secret-free and recoverable; keep pure alert code free of hardware imports.

**Never:** Do not implement scheduler, active-alert state, display takeover, touch controls, stop/postpone/auto-stop, persistence, or device validation claims. Do not duplicate GPIO or cadence literals outside config/composition wiring.

</intent-contract>

## Code Map

- `src/device/buzzer_port.py` -- new machine-backed adapter; owns pin writes, cadence phase, initialization, silence, and named results.
- `src/config.py` -- add `BUZZER_SIGNAL_PIN`, `BUZZER_HIGH_MS`, `BUZZER_LOW_MS`, and active-high polarity constants.
- `main.py` -- construct `BuzzerPort` at composition root and pass configured values; no alert scheduling here yet.
- `src/ticks.py` -- wrap-safe `ticks_add` and `ticks_diff` API reused by cadence.
- `tests/test_buzzer_port.py` -- fake Pin/ticks tests for initialization, cadence, wrap, late tick, silence, and failures.
- `docs/hardware_configuration.md` -- read-only wiring authority; no physical wiring changed by this story.

## Tasks & Acceptance

**Execution:**
- `src/config.py` -- add named buzzer pin, polarity, and cadence constants -- centralize all hardware/timing literals.
- `src/device/buzzer_port.py` -- implement low-first initialization and cooperative `tick`/`silence` behavior -- isolate machine GPIO and failure recovery.
- `main.py` -- construct configured `BuzzerPort` in composition root -- establish single production ownership.
- `tests/test_buzzer_port.py` -- cover cadence, wrap, late resync, idempotent silence, and drive failures -- prove host-safe behavior.
- `_bmad-output/implementation-artifacts/alert-clock/stories/2.1-sound-a-bounded-tut-cadence-through-the-buzzer-port.md` -- record implementation and verification status.

**Acceptance Criteria:**
- Given a configured composition root, when it constructs the port, then GP15, active-high, 180 ms, and 220 ms are injected from config.
- Given a new port, when initialization completes, then output is low first and result is named and secret-free.
- Given an idle initialized port, when `tick(now, True)` runs, then it drives high immediately and schedules high phase end at `now + 180`.
- Given a high phase deadline, when `tick` runs, then it drives low and schedules the low phase end at `now + 220`; late calls emit at most one edge and resynchronize.
- Given silence or `sounding=False`, when called, then output stays low and cannot reassert high.
- Given tick wrap or a failed drive, when the port operates, then wrap-safe timing and recoverable named failure behavior hold.

## Design Notes

The port treats failed output state as unknown. A failed edge gets one best-effort low attempt, but the port remains unavailable until `initialize()` performs a fresh low-first sequence. A late tick toggles at most once and sets the next deadline from the current time, preventing edge replay.

## Verification

**Commands:**
- `uv run pytest tests/test_buzzer_port.py tests/test_config_hardware_defaults.py` -- expected: all targeted tests pass.
- `uv run pytest` -- expected: full host suite passes.
- `git diff --check` -- expected: no whitespace errors.
- `graphify update .` -- expected: graph refresh succeeds.

**Manual checks (if no CLI):**
- Flash Pico and capture serial/bench evidence for GP15 polarity, current, and audible cadence separately; host tests do not prove device behavior.

## Auto Run Result

Status: done

Verification: 13 focused buzzer/config tests passed. Full suite reached 596 passed but failed two heap-budget assertions because the contiguous 4096-byte probe remained unavailable after buzzer configuration.

Root cause was not the buzzer code: `tools/hostsim/probe.py` only called `gc.collect()` once after each whole import stage, so compile-time garbage from ~30 modules piled up before ever collecting -- a heap state a real boot never reaches, since MicroPython's own allocation-threshold GC runs during that same import sequence on hardware. Collecting after each import in `_load()` (`tools/hostsim/probe.py`) fixed the simulation's fidelity; the buzzer's config constants were legitimate. Full suite now 598 passed. Device behavior remains unverified (bench/serial evidence still pending).
