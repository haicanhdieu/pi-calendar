---
topic: Wi-Fi Provisioning & Admin Config UX
updated: 2026-09-06T21:47
---

- (decision) Setup page and Config page get distinct visual styles: Setup page = minimal/utility (captive-portal feel), Config page = more considered app-like, since it's used long-term.
- (decision) Web UI palette is dark mode, but deliberately distinct from Device's Forest & Amber (avoid confusing the two interpretive systems).
- (decision) Config page form-factor: mobile browser only, no desktop responsive requirement for v1.
- (decision) Working mode: fast path — batch draft both spines with [ASSUMPTION] tags, skip creative tools.
- (event) User switched session language to English mid-discovery.
- (event) Drafted DESIGN.md and EXPERIENCE.md (fast path, status: draft) with [ASSUMPTION] tags pending user review.
- (decision) Setup flow: two screens, one submit. Screen 1 = SSID pick + Wi-Fi password (Next, no save yet). Screen 2 = Admin password (Connect = actual submit: saves both, drops AP, joins network). Matches FR-1's single-submit-saves-both sequencing.
- (decision) No explicit logout control on Config page. Single-owner trusted LAN device — session persists until tab close or idle timeout only.
- (decision) Empty SSID scan copy confirmed as drafted: 'No networks found — move closer to your router and reload' + manual text-link 'Rescan' action.
- (decision) Accent color locked: Violet Indigo #7c6cf6 (calm, premium, modern), chosen from color-themes-1.html render of 6 options. Replaces placeholder #4f8cff.
- (event) Finalize: reviewer gate skipped (user declined validation lens menu — hobby project, low stakes).
- (assumption) Non-blocking assumptions carried into build: (1) no localization beyond English, (2) Wi-Fi password field clears on join failure but Admin password field does not, (3) AP-drop-mid-fill reloads to fresh SSID picker with no partial-state persistence, (4) browser back-button not specially handled on Setup page, (5) Config session stays valid after an admin password change (no forced re-login).
- (event) Rendered 4 key-screen mocks: key-setup-ssid, key-setup-admin, key-config-login, key-config-settings (.working/).
- (decision) Mock coverage confirmed sufficient (all 4 IA surfaces mocked). Promoted key-screen mocks from .working/ to mockups/.
- (event) spines finalized
