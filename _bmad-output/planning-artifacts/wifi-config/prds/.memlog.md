---
topic: wifi-config
updated: 2026-09-06T20:40
---

- (event) New slug 'wifi-config' created for Wi-Fi provisioning + admin config web UI, its own full PRD/UX/architecture/epics/implementation/test cycle, separate from pico-w-calendar-clock
- (decision) Stakes: hobby/personal, fast path, batched gaps with [ASSUMPTION] tags (carried over from pico-w-calendar-clock working mode)
- (decision) Setup AP: open (no password), fixed SSID PiCalendar-Setup
- (decision) Fallback to setup mode: retry saved Wi-Fi 3 times then re-enter AP setup
- (decision) No admin-password recovery path in v1 (no physical button yet); factory reset mechanism deferred to later phase
- (decision) Color scheme setting is v1 stub: UI shows setting, only current theme selectable
- (decision) Device shows its own IP/gateway address on its TFT screen, both in Setup AP mode and after joining home Wi-Fi
- (event) Draft prd.md written (fast path), cross-references pico-w-calendar-clock/prds/prd.md for shared Device/Glossary context
- (event) Reviewer gate (rubric walker): 0 critical, 1 high, 2 medium, 2 low. Fixed high (FR-4 citation pointed at wrong sibling doc, corrected to DESIGN.md), both mediums (FR-2 'briefly' pinned to 10s, FR/UJ ID-namespace note added to §0); folded low finding on FR-1 join timeout (15s) as bonus fix; skipped low NOTE-FOR-PM stylistic suggestion per reviewer's own note that it's non-required
- (event) Polish pass (structure, prose): structure clean/preserve; one prose fix in FR-1 description (self-contradictory 'no keyboard/screen input beyond its own TFT' -> 'no input mechanism beyond its own TFT screen')
- (decision) Open Question 'is open Setup AP security tradeoff acceptable' resolved: yes, already implicit in the FR-1 design choice — not a real blocker, just needed explicit closure
- (decision) Deferred non-blockers: on-screen-only IP display (revisit if impractical in daily use), factory-reset timeline (revisit when physical button hardware exists)
- (event) PRD finalized
