---
scope: Wi-Fi provisioning and authenticated local admin configuration on the existing Pico W clock
purpose: build-substrate
altitude: feature
updated: 2026-09-06T22:29
---

- (constraint) Parent AD-1: pure core must not import device APIs; provisioning domain decisions and HTTP parsing are host-testable, while WLAN/TFT persistence adapters remain in src/device/.
- (constraint) Parent AD-2, AD-3, AD-5, AD-8, AD-9, AD-10, AD-11, and AD-12 bind this feature: App is sole product-state writer; time/RTC ownership stays with App; scheduling uses ticks; network failure must not stop rendering; secrets stay untracked; production core is CPython-compatible; display uses DisplayPort; App/compositor owns display lifecycle.
- (direction) Fast path selected; output is an implementation-focused architecture spine only.
- (assumption) AP connection timeout is 15 seconds; no captive portal, mDNS, reset mechanism, or desktop layout in v1, inherited from PRD scope.
- (question) Admin password representation, session lifetime, and exact persistence backend are not fixed by the PRD and require an architecture decision or deferred boundary.
- (version) MicroPython Pico W firmware v1.29.0 is current stable as of 2026-09-06; official RP2 quick reference confirms AP-mode WLAN, scan, STA connection, and infinite default reconnect behavior.
- (decision) AD-1: extend the existing functional-core/imperative-shell paradigm with a cooperative network owner; one NetworkCoordinator tick owns STA/AP state, the HTTP listener, and connection work, returning immutable provisioning/status events to App so rendering stays responsive.
- (decision) AD-2: explicit NetworkMode state machine owns transitions: boot loads persisted settings; valid credentials enter station-connect; three terminal connection failures enter setup AP; setup form submit stages a candidate, attempts a bounded 15s station join, atomically persists only after join succeeds, and otherwise restores setup AP.
- (decision) AD-3: SettingsStore is the sole persistence boundary; it owns one versioned device-local record containing Wi-Fi SSID/password, salted admin-password verifier, and color-scheme ID. It writes via temp-and-replace and validates a complete record before exposing it; no secret reaches config.py, logs, HTML, or pure modules.
- (decision) AD-4: Web boundary uses a strict allowlisted HTTP router with bounded request/header/body sizes, form-urlencoded POST bodies only, fixed local static assets, explicit escaped HTML, no external dependencies, and one request handled in bounded incremental socket ticks.
- (decision) AD-5: Auth has a single admin verifier, constant-time comparison where available, and opaque server-side session records held only in RAM; cookie is HttpOnly and SameSite=Strict. Successful password change replaces verifier and invalidates all sessions except the request session. HTTP is intentionally unencrypted and therefore only accepted as a constrained local-network/setup-AP v1 threat boundary.
- (decision) AD-6: App consumes NetworkEvent values and owns visible device status; setup mode renders SSID and 192.168.4.1, station-connected/reconnected renders assigned IP for at least 10 seconds. Web outcomes never render TFT directly.
- (decision) AD-7: host tests use fakes for WLAN, sockets, filesystem, clock/ticks, and display status port; no pure provisioning/domain/HTTP parsing module imports machine, network, socket, or time-specific hardware packages. Physical AP coexistence, AP address, and flash persistence require device evidence.
- (question) BLOCKER: parent AD-8 mandates worker ownership while existing code and child AD-1 use a cooperative NetworkCoordinator; parent must be amended or the feature refactored into its worker.
- (question) BLOCKER: parent AD-9 makes secrets.py the Wi-Fi source while child SettingsStore needs to supersede it; choose and record a migration/supersession in parent before implementation.
- (direction) Reviewer gate fixed overlay ownership, setup POST candidate/result handshake, AP lifetime through terminal response, bounded request/session contract, and power-loss persistence proof requirement.
- (event) reviewer gate run; finalization held for two inherited-parent conflicts
- (constraint) User directed that wifi-config is the authoritative PRD/spine for provisioning and admin configuration; pico-w-calendar-clock core architecture references it only.
- (decision) Parent AD-8 and AD-9 were amended to make this spine authoritative for cooperative network ownership and SettingsStore; inherited conflicts are resolved.
- (event) parent references reconciled; spine finalized with wifi-config authoritative for Wi-Fi provisioning and admin configuration
- (decision) Final adversarial gate closed persistence split-state: after a successful candidate STA association, commit failure disconnects candidate STA, preserves setup AP and retry form, emits named persistence failure, and never emits online/IP state or resets failure count.
- (event) final reviewer gate passed after persistence-failure seam fix
- (decision) AD-3/AD-5 amendment: version 1 settings use PBKDF2-HMAC-SHA256 with a 16-byte os.urandom salt, 32-byte derived verifier, and 20,000 iterations under verifier_version pbkdf2-sha256-v1. The implementation owns a portable HMAC-SHA256/PBKDF2 core compatible with MicroPython v1.29 hashlib; it performs at most 200 PBKDF2 rounds per coordinator tick so login/password change never monopolizes rendering. Salt and verifier are binary-encoded only at the persistence boundary; plaintext input is discarded after the result.
- (decision) AD-5 amendment: session table holds at most 4 opaque 16-byte IDs drawn from os.urandom, each with a 15-minute sliding idle deadline using ticks_ms. New login evicts the least-recently-used expired entry first, otherwise rejects with a fixed service-unavailable response; session cookies have no persistence attribute and use HttpOnly, SameSite=Strict, Path=/. Password change retains only the requesting valid session and invalidates every other session.
- (version) MicroPython v1.29 documentation confirms builtin hashlib and os.urandom; os.urandom returns hardware-generated bytes whenever possible. PBKDF2 is implemented locally from HMAC-SHA256 to avoid an unsupported library dependency.
- (decision) AD-3/AD-5 seam closure: exactly one correlated KdfJob with global 200-round tick budget and fixed busy admission; canonical version-1 JSON record and tmp/backup recovery protocol; canonical unpadded-base64url session ID, ticks_diff/ticks_add expiry rule, invalid-cookie response, and password-change commit-before-session-mutation ordering.
- (assumption) Before hardware credentials or sessions are accepted, the flashed MicroPython v1.29 image must smoke-test hashlib.sha256, os.urandom(16), and tick-wrap session expiry. Failure blocks the device claim but not host implementation/testing.
- (decision) AD-3 recovery seam closure: each commit removes only the stale backup after the temp record is durable, then rotates current to backup. A valid backup restores only when current is absent, or after an invalid/unsupported current is moved into an empty quarantine; an existing quarantine stops restoration and enters setup, preserving all evidence.
