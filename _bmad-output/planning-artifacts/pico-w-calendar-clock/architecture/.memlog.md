---
scope: Pico W calendar clock firmware
purpose: build-substrate
altitude: feature
updated: 2026-09-06T22:00
---

- (direction) Fast path selected; architecture spine is the only deliverable.
- (constraint) Source priority: finalized UX and hardware_configuration.md supersede older draft brief/PRD/handoff conflicts; v1 is 320x240 landscape, seconds-level, no input, and clock-dominant automatic rotation.
- (constraint) Hardware pins, SPI0 mapping, 40 MHz bus, and MADCTL 0xA8 are fixed by the tested physical wiring; a change requires matching hardware documentation.
- (version) MicroPython v1.29.0 is the latest stable Pico W firmware as of 2026-09-06, verified on the official MicroPython Pico W download page; use release builds, not previews.
- (decision) AD-1 binds all firmware layers and prevents hardware imports leaking into testable logic: use Functional Core / Imperative Shell, with pure calendar/date/view-state code depending on no device modules and hardware adapters at the outer edge.
- (decision) AD-2 binds orchestration, view switching, rendering, and sync and prevents competing writers: one single-threaded application loop owns mutable application state; interrupts, threads, renderers, and adapters do not mutate it directly.
- (decision) AD-3 binds time sync and both views and prevents disagreement about date or trust: TimeService is the sole owner of authoritative UTC wall time and sync metadata, publishes immutable local TimeSnapshot values, and alone derives synced versus unsynced.
- (decision) AD-4 binds NTP, timezone conversion, rollover, and future RTC and prevents double offsets: device time is stored and synchronized as UTC; pure logic applies fixed Asia/Ho_Chi_Minh UTC+07:00 when constructing local snapshots; scheduling never uses wall time.
- (decision) AD-5 binds view dwell, redraw cadence, and retry scheduling and prevents wall-clock jumps or counter wrap bugs: deadlines use MicroPython ticks_ms with ticks_add and ticks_diff; durations come from configuration.
- (decision) AD-6 binds Gregorian generation, future lunar enrichment, and calendar rendering and prevents a Phase 5 return-type rewrite: calendar core emits MonthGrid containing seven-cell Monday-first weeks of DayCell records; each cell carries Gregorian date, in-month, today, and an annotations collection that is empty in v1.
- (decision) AD-7 binds the display driver and both renderers and prevents RAM exhaustion and long-uptime fragmentation: render directly through bounded dirty regions with reusable buffers; no full 320x240x16 framebuffer and no recurring allocation in the one-second update path.
- (decision) AD-8 binds network failures, display continuity, and error handling and prevents NTP availability from becoming device availability: adapter operations are time-bounded; recoverable network errors become state plus serial diagnostics and never terminate or permanently block the render loop.
- (decision) AD-9 binds configuration and credential ownership and prevents secrets entering source or domain code: checked-in config owns non-secret hardware/product defaults; an ignored device-local secrets module is consumed only by the Wi-Fi adapter and absence is handled as unsynced operation.
- (decision) AD-10 binds host tests and device deployment and prevents CPython/MicroPython drift: pure modules use the common language subset and are tested unchanged under CPython; machine, network, and ntptime appear only in device adapters and composition root.
- (assumption) ILI9341 remains the controller assumption because the current driver and tested orientation work, but the controller identity has not been read from the physical panel; isolate this behind the display adapter and verify before declaring the driver final.
- (assumption) Configuration defaults for fast-path drafting: 30 seconds clock dwell, 8 seconds calendar dwell, and 1 hour NTP retry; behavior must not hard-code them.
- (question) Time trust freshness threshold after a prior successful sync is not specified; defer until acceptance criteria define when a still-ticking RTC becomes stale.
- (question) Calendar month rollover during an active calendar dwell conflicts in the UX artifact; defer exact behavior to PRD/UX reconciliation while keeping MonthGrid generation independent of rendering.
- (event) Fast-path architecture decisions drafted from PRD, brief, UX, hardware docs, project structure convention, and current main.py.
- (decision) Amend AD-2 and AD-3: AppState is the sole mutable product-state store; pure TimeService functions return replacement TimeState and TimeSnapshot values, and only the application loop commits them.
- (decision) Amend AD-3: TimeSnapshot has exact UTC/local DateTime records or None, trust synced/unsynced, and sync_age_ms or None; on cold boot with no valid time it is unsynced with absent date-times, the clock shows placeholders, Calendar is not entered, and successful sync starts rotation.
- (decision) Amend AD-5 and AD-8: both successful periodic resync and failure retry are scheduled; Wi-Fi/NTP is an incremental start/poll/cancel adapter with explicit pending or SyncResult output so no call waits across a render deadline.
- (decision) Amend AD-6: CalendarAnnotation has exact kind and text fields, ordered by the core; provider enrichment is cached outside renderers and future solar terms compute at most once per local date.
- (decision) AD-11 binds renderers and display adapter: DisplayPort exposes width, height, fill_rect, measure_text, and draw_text with top-left integer coordinates, clipped half-open rectangles, RGB565 integer colors, and stable font identifiers; the adapter owns SPI translation and buffers.
- (decision) AD-12 binds view lifecycle and shared status: clock-first plain-cut 30s/8s rotation, shared UNSYNCED overlay on both views, no rotation until time exists, same-month rollover updates highlight, and month rollover during Calendar cuts to Clock before rebuilding next entry.
- (constraint) SPI display baseline is mode 0 at 40 MHz, RGB565 high byte first, with fill rectangles expressed as x/y/width/height and translated to inclusive controller window endpoints.
- (constraint) Credential file must not be created until root .gitignore excludes /secrets.py; a checked-in secrets.example.py may document names without values.
- (constraint) Final UX supersedes earlier brief proposals: no button in v1 and textual UNSYNCED badge on both views.
- (question) Alarm remains deferred beyond v1 and should be revisited before other unrelated expansion because the source brief marks it as an intended direction.
- (event) Independent rubric, currency, adversarial, and source-reconciliation reviews completed; clear fixes selected for redistillation.
- (decision) Amend AD-3: ClockPort backed by machine.RTC exposes read_utc and set_utc; App marks RTC valid only after a successful NTP result in this boot, then reads it for each snapshot so hardware advances wall-time between syncs.
- (decision) Amend AD-6: annotations are semantic CalendarAnnotation kind/value records; models.py owns the allowed kind registry and ordering, providers cannot invent kinds, and renderers own display formatting.
- (decision) Amend AD-8: use one isolated RP2040 network worker for blocking WLAN/DNS/NTP, with a fixed-capacity lock-protected command/result mailbox; only the main loop writes RTC or AppState, and bundled ntptime.settime is not used.
- (assumption) MicroPython _thread is experimental in v1.29.0; verify the single network worker and lock mailbox on the flashed Pico W before FR-1 implementation proceeds, otherwise update AD-8 rather than adding blocking calls to the render loop.
- (decision) Amend AD-12: defaults are 30s/8s but configurable; UiCompositor owns status visibility and forces full active-view repaint when badge visibility changes; loop reduction order is adapter results, time snapshot, date rollover, view deadline, base render, status overlay.
- (event) Second reviewer pass identified final contract holes; selected deterministic fixes for time advancement, concurrency, annotation ownership, badge restoration, and event order.
- (decision) Amend AD-8: SyncCommand and SyncResult both carry monotonically increasing command_id; only matching non-expired results apply, and every failed or expired sync immediately transitions TimeState trust to unsynced while RTC continues.
- (event) Final adversarial schema hole closed; experimental network worker retained as an explicit flashed-device proof gate.
- (decision) Amend AD-8: define separate capacity-one command and result slots guarded by one lock; App permits exactly one in-flight command, never blocks enqueue/dequeue, consumes and discards stale results, and does not enqueue retry until the worker publishes the prior command's terminal result and returns idle.
- (decision) Amend AD-8: worker must publish exactly one terminal result per accepted command, enforce the command deadline with bounded WLAN/socket operations, and leave the result slot untouched until App consumes it; mailbox saturation is a contract violation logged as fatal diagnostic, never silent overwrite.
- (event) Final mailbox direction, capacity, backpressure, and timeout lifecycle specified after adversarial review.
- (event) spine finalized
- (direction) Wi-Fi provisioning and admin configuration are now governed by the wifi-config PRD and its architecture spine; the core clock spine references that feature and does not own its provisioning/persistence decisions.
- (decision) Amend AD-8: core time sync shares the Wi-Fi feature's cooperative NetworkCoordinator boundary; feature-specific WLAN/AP/HTTP ownership and recovery are defined by wifi-config architecture, while the core retains only non-blocking rendering/time-ownership requirements.
- (decision) Amend AD-9: wifi-config SettingsStore is the authoritative device-local Wi-Fi/admin settings source; legacy secrets.py is not a production credentials source after migration policy is chosen in the Wi-Fi feature spine.
- (event) core spine amended to reference wifi-config as authoritative Wi-Fi architecture
